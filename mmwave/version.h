/*
 * version.h — MMwave radar node firmware version.
 *
 * Independent version track per repo rule (CLAUDE.md "Versioning") — never
 * aligned to any other board. Registry entry: firmware/registry.json id
 * "mmwave" (added when the registry-driven build lands, #902).
 */

#ifndef MMW_VERSION_H
#define MMW_VERSION_H

#define MMW_MAJOR 0
#define MMW_MINOR 1
#define MMW_PATCH 0

#endif  // MMW_VERSION_H
